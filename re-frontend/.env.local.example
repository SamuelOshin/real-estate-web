# Copy to .env.local and fill in once the FastAPI backend exists.
# Referenced by src/app/api/auth/*/route.ts once they're switched from
# mock implementations to real backend proxying (see the comments in
# those files for exactly what changes).
BACKEND_API_URL=http://localhost:8000
